import { FilenameArrayPipe } from './filename-array.pipe';

describe('FilenameArrayPipe', () => {
    it('should be defined', () => {
        expect(new FilenameArrayPipe()).toBeDefined();
    });
});
